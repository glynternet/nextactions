server {
    listen 443 ssl;
    server_name {{DOMAIN}};
    ssl_certificate     /etc/ssl/{{DOMAIN}}.pem;
    ssl_certificate_key /etc/ssl/{{DOMAIN}}.key;

    root /var/www/;
    index index.html;

    location / {
        try_files $uri /index.html;
    }
}